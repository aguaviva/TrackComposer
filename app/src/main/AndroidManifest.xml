<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trackcomposer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ApplicationClass"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ActivityPercussion"
            android:label="@string/title_activity_note"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ActivityPianoRoll"
            android:label="@string/title_activity_percussion"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ActivityChord"
            android:label="ActivityChord"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".InstrumentChooser"
            android:label="ActivityInstrument"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".ActivityMain"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>